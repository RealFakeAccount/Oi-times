#include <cstdio>
#include <cstring>
#include <iostream>
#include <algorithm>
#include <vector>
#include <queue>
using namespace std;
typedef pair<int, int> P;
#define fir first
#define sec second
const int MAXN = 4e4 + 20;
const int MAXM = 64 + 2;

int N, M, K;
int a[10], b[MAXM];

void bfs()
{
	memset(dis[])
}

int main()
{
	cin>>N>>M>>K;
	for(int i = 1; i <= K; i++)
		scanf(" %d", &a[i]);
	for(int i = 1; i <= M; i++)
		scanf(" %d", &b[i]);

	return 0;
}